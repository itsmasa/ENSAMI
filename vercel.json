{
    "builds": [
      {
        "src": "chatbot.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "chatbot.py"
      }
    ],
    "env": {
      "OPENAI_API_KEY": "@openai-api-key"
    }
  }